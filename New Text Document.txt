echo "# myRepository" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add baitap https://github.com/Produck993/myRepository.git
git push -u origin main
git checkout -b main (change maste
git remote rm origin